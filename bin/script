#!/usr/bin/env ruby
require '../lib/parser.rb'
require '../lib/calculator.rb'

command = gets.strip
parser = Parser.new
while command != 'exit'
  puts parser.parse(command)
  command = gets.strip
end
